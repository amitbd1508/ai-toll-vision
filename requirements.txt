# ---- Core (required) ----
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
PyYAML==6.0.2
python-multipart==0.0.20
numpy>=1.26,<3
opencv-python-headless>=4.9

# ---- Detection / tracking (required for real, non-mock detection) ----
# NOTE: torch is large (CPU wheel ~200MB+, CUDA wheel much larger). Install
# separately per https://pytorch.org/get-started/locally/ for your platform
# before installing ultralytics, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
ultralytics>=8.3

# ---- Optional: license plate OCR (privacy-sensitive, opt-in via config.yaml ocr.enabled) ----
easyocr>=1.7 ; extra == "ocr"

# ---- Optional: SAM2 segmentation (opt-in via config.yaml segmentation.enabled) ----
# SAM2 has no stable PyPI release at time of writing; install from source:
#   pip install "git+https://github.com/facebookresearch/sam2.git"
# sam2 ; extra == "segmentation"

# ---- Optional: local VLM reasoning (opt-in via config.yaml vlm.enabled) ----
transformers>=4.46 ; extra == "vlm"
accelerate>=1.0 ; extra == "vlm"
qwen-vl-utils ; extra == "vlm"

# ---- Testing ----
pytest>=8.0
httpx>=0.27
